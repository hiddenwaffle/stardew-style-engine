// "Camera" is the idea that 0, 0 on the canvas = x, y on the current map.

export function translateScreenToWorld(x: number, y: number): [number, number] {
  // TODO: Do it
  console.log(x, y);
  return [0, 1];
}
